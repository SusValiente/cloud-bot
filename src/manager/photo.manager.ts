/**
 * Class representing Photo Manager
 *
 * @export
 * @class PhotoManager
 */
export class PhotoManager {
    public static async managePhoto(context: any): Promise<void> {
        console.log(context);
        return Promise.resolve();
    }
}
